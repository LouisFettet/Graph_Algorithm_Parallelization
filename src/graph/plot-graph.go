//plotgraph.go

package graph

/*
func getPoints(nodelist []Node) plotter.XYs {
	points := make(plotter.XYs, len(nodelist))
	for node := range nodelist {
		points[0].X = float64(node.X)
		points[0].Y = float64(node.Y)
	}
	return points
}

func GeneratePlot() {
	p, err := plot.New()
	if err != nil {
		panic(err)
	}

	p.Title.Text = "Graph Test"
	p.X.Label.Text = "X"
	p.Y.Label.Text = "Y"
	p.Add(plotter.NewGrid())

	l, err := plotter.NewLine(lines)
	if err != nil {
		panic(err)
	}

	if err != nil {
		panic(err)
	}

	if err := p.Save(10, 10, "test.png"); err != nil {
		panic(err)
	}
}
*/
